return {
  "f-person/git-blame.nvim",
  config = function()
    -- Optional: Configure the plugin here if needed
    -- For example, to enable virtual text blame:
    vim.g.gitblame_enabled = 1
  end
}
